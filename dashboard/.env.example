# Point the dashboard at the Tracer backend for login and repo data.
# Copy to .env and restart the dev server (npm run dev).
# Backend must be running (e.g. cd backend && node server.js).
VITE_API_BASE=http://localhost:3002
